1. На локальном репозитории сделать ветки для:
- Postman
git branch Postman

- Jmeter
git branch Jmeter

- CheckLists
git branch CheckLists

- Bag Reports
git branch Bug_Reports

- SQL
git branch SQL

- Charles
git branch Charles

- Mobile testing
git branch Mobile_testing

2. Запушить все ветки на внешний репозиторий
git push origin --all

3. В ветке Bag Reports сделать текстовый документ со структурой баг репорта
git checkout Bag_Reports

cat > bagreport.txt
Bug_id: 70
Title: Grid_data.txt file doesn’t attached when sending the message
Severity: minor
Priority: low
Environment: 1) Pixel XL, Android 9       
Precondition: Install and open app. Press “Save data”.
STR: 1) Press “Send email”
	2) Press “Messages”
	3) Choose number to send file
AR: Appears message window with attached Grid_data.txt file
ER: Appears a message “Attachment type not supported”. After in the outgoing message there is text “Android device info”    
^C

4. Запушить структуру багрепорта на внешний репозиторий
git add bagreport.txt; git commit -m "add file"
git push --set-upstream origin Bug_Reports (Предложил команду сам комп, не давал запушить через git push)

5. Вмержить ветку Bug Reports в Main
git checkout master
git merge Bag_Reports -m "merge"

6. Запушить main на внешний репозиторий.
git push

7. В ветке CheckLists набросать структуру чек листа.
cat > checklist.txt
Чек лист на установку приложения с AppleStore:

1. Найти приложение в магазине
2. Скачать
3. Проверить появилась ли иконка на главном меню
4. Установка уже ранее установленного приложения
5. Проверить, требует ли приложение одобрить использование локации\камеры\диктофона
6. Можно ли найти приложение через поиск внутри телефона
^C

8. Запушить структуру на внешний репозиторий
git add checklist.txt
git commit -m "new file"
git push --set-upstream origin CheckLists


9. На внешнем репозитории сделать Pull Request ветки CheckLists в main
Comparing changes -> Create pull request  -> merge pull request

10. Синхронизировать Внешнюю и Локальную ветки Main
git checkout master
git fetch
git pull

